# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
python-dotenv>=1.0

# --- Imagem (leve) ---
pillow>=10.2
numpy>=1.26

# --- Visao computacional (deteccao + embedding ArcFace) ---
insightface>=0.7.3
onnxruntime>=1.17           # troque por onnxruntime-gpu se tiver CUDA

# --- Classificador "rosto real" (CLIP zero-shot) ---
open-clip-torch>=2.24
torch>=2.2                  # CPU ja funciona; CUDA recomendado p/ velocidade

# --- Provider de geracao ---
replicate>=0.25

# --- Testes ---
pytest>=8.0
httpx>=0.27
